# Documentation synchronization status

## Current phase

`COMPLETE` — exhaustive bilingual synchronization is complete, every inventory has zero remaining items, and every machine-checkable final validation criterion is satisfied.

## Baseline and evidence

- Application baseline: `debd15e3600e9c140b23756d849dc40964a27910`
- Documentation baseline: `2542c12490cf469539032aa246dfaeee3019e1b3`
- Both repositories were clean when captured; exact gitlinks are in [source-baseline.md](source-baseline.md).
- Canonical architecture reading is complete.
- Existing documentation structure/configuration and the application page/API/migration/extension registrations were inspected.
- Generated inventories currently contain 291 page/layout/middleware/component rows, 428 API rows, 212 registered-migration/material-data rows, 106 host/installed-extension rows, 16 domain rows, 10 configuration rows, and 102 historical audit-impact rows.
- Added paired operator references for runtime configuration, deployment/recovery, and background work, plus paired developer references for data integrity and the validation/i18n error contract.
- Added generated, paired route tables for all 428 core handlers across ten API groups. Each row records method/path, direct authorization and validation landmarks, and source; the tables explicitly remain navigation evidence until their ledger rows are manually traced.
- Corrected the root English redirect and aligned documentation tooling/workflow metadata to Bun 1.3.13.
- Generated a paired safe historical audit-impact register for all 102 finding, investigation, and browser-persona records. It records current documentation consequences without reproducing obsolete vulnerable behaviour.
- Completed direct source/helper/schema/UI tracing for the auth, role, and user families. Added credential-user activation and append-only transactional security-audit guidance in both languages.
- Marked 24 identity API rows and 15 identity page/component/middleware rows `DOCUMENTED_VERIFIED`; all other rows remain open.
- Traced the application shell and corrected stale navigation claims: `system:read` gates Common Admin/dump, Programs and Roles are currently always listed, exact Teams provide only boolean Agreement/Proponent navigation hints, no Help/Feedback links exist, and the bell has no rendered notification workflow. Rewrote both navigation guides with current table, modal, lookup, locale, logout, responsive, error, and troubleshooting behaviour.
- Marked eight directly traced shell/shared-component rows `DOCUMENTED_VERIFIED`; the broader platform domain remains open pending the remaining shared-component/accessibility sweep.
- Reconciled the agency-administration domain against all 35 agency handlers, the agency schemas and migrations, transaction/authorization paths, 15 page/component rows, the manual server-route ledger, and focused tests. Replaced both agency guides with matched coverage of exact scope rules, all ten tabs, supported CRUD actions, uniqueness and validation constraints, soft deletion, concurrency, recovery, and dependency order.
- Marked 35 agency API rows and 15 agency page/component rows `DOCUMENTED_VERIFIED`. Agency migration/material rows remain open for the dedicated database-integrity pass, so the broader agency domain row is not yet terminal.
- Completed the program-profile slice of transfer-payment design: profile list/detail and six-tab navigation, profile CRUD, transactional wizard, outcomes, objectives, fiscal-year budgets, budget allocation guards, performance indicators, exact scope masking, extension-aware deletion, and recovery behavior are reconciled in both languages.
- Marked 26 program-level transfer-payment API rows and 19 program page/component rows `DOCUMENTED_VERIFIED`. Stream handlers, pages, and setup designers remain open as the next transfer-payment slice.
- Completed the core stream slice: the paired guide now matches the actual 19-tab shell, adds the omitted holdback-base and workflow-setup surfaces, and covers stream identity, wizard, core reference/financial configuration, exact ownership, extension-aware soft deletion, concurrency, and recovery.
- Marked 54 core stream API rows and 36 stream page/component rows `DOCUMENTED_VERIFIED`. Specialized review, assessment, recommendation, approval, workflow, document-template, and extension rows remain open for their dedicated lifecycle passes.
- Completed the stream review/assessment configuration slice: review setup/member CRUD, draft activation, active pending publication, versioned snapshots, schema creation/association, assessment editor structure, dependencies/calculations/weights/outcomes/impactors, and assessment-set API compatibility are reconciled in both languages.
- Marked 23 review/assessment API rows and 41 mounted page/component rows `DOCUMENTED_VERIFIED`. Marked the three retained but source-wide unmounted Assessment Set UI components `NOT_APPLICABLE_VERIFIED`; the guide explicitly distinguishes their supported API contracts from the absence of a current navigation path.
- Added paired Checklist Schemas guides and navigation. They cover exact stream access, editor sections, bilingual question/answer/help fields, comment policies, nested result modes and thresholds, draft activation, working-copy publication, immutable versions, pinned runtime snapshots, validation, and recovery.
- Marked four checklist-schema API rows and seven editor page/component rows `DOCUMENTED_VERIFIED` and remapped their inventory destinations to the new paired guide. Runtime checklist rows remain open for the runtime-review pass.
- Added paired Recommendation Schemas and Setups guides and navigation. They cover the schema form model, exactly one deciding result question, published schema versions, setup/member ordering, approval dependencies, activation/pending publication, pinned plans, runtime outcome derivation, deletion, and recovery.
- Marked 14 recommendation schema/setup API rows and 11 administrator page/component rows `DOCUMENTED_VERIFIED`, with destinations remapped to the dedicated guide. Runtime recommendation form/workflow routes remain open for the workflow-runtime pass.
- Reconciled approval-template configuration and publication in the paired guide, including stored-scope authorization, header/step/certification/additional-approval working state, draft activation, active pending publication, versioned snapshots, runtime immutability, validation, and recovery.
- Marked seven approval-template API rows and 11 mounted shared/stream page/component rows `DOCUMENTED_VERIFIED`. Marked four superseded unmounted transfer-payment-local editor components `NOT_APPLICABLE_VERIFIED`; the current stream UI delegates to `Common/ApprovalTemplates`.
- Corrected and expanded stream document-template guidance: HTML templates support native HTML and/or PDF output, both bilingual source files are required, per-file/request size limits are explicit, extension-only kind validation is identified as a trusted-author boundary, kind immutability, replacement rollback/cleanup, downloads, soft deletion, and historical generated-document separation are covered.
- Marked five stream document-template API rows and the mounted tab row `DOCUMENTED_VERIFIED`; agreement-side template lookup/generation remains open for the agreement documents pass.
- Added paired Workflows guidance and navigation after tracing setup CRUD, draft activation, pending publication, embedded dependency snapshots, runtime applicability, stage ordering, source-status transitions, cancellation, history, and pinned retry lineage.
- Marked all seven stream workflow-setup API rows, all six workflow-runtime API rows, and six mounted setup/runtime page-component rows `DOCUMENTED_VERIFIED`; runtime review, approval, completion, and recommendation-component rows outside this slice remain open.
- Added paired Runtime Reviews guidance and navigation after tracing assessment/checklist retrieval and persistence, pinned definitions, scoring/result derivation, custom outcomes and alignment, additional-reviewer ownership, strict completion handoff, locked states, review-set/workflow advancement, and denied-review retry.
- Marked 11 runtime review/additional-reviewer API rows and 12 mounted assessment/checklist/shared runtime UI rows `DOCUMENTED_VERIFIED`. Generic completion and approval route/component rows remain open for their dedicated runtime pass; configuration-only assessment/checklist editors remain separate rows.
- Rewrote the paired Approvals and Completions concept guide against the generalized runtime adapters. It now covers supported entity types, exact access, strict completion, materialization, current-assignee decisions, certifications, on-behalf requirements, reassignment, fractional inserted steps, terminal synchronization, idempotent advancement, history, and recovery.
- Marked all eight generalized approval API rows, both completion API rows, and all six mounted shared approval/completion UI rows `DOCUMENTED_VERIFIED`.
- Closed the remaining shared recommendation/review presentation rows after direct component tracing: Recommendation Form maps to workflow guidance, while Review Schema General Fields and Review Set Setup Fields map to the paired schema guide. Their focused component tests passed 5/5.
- Replaced the paired Common Administration guides with natural bilingual, source-matched coverage of the exact 19-tab order, five read-only resource families, route-addressable state, search/pagination/deleted filters, generated controls, lookup hydration, Canadian address behaviour, JSON handling, global versus scoped lookup authorization, fresh-write locking, schema snapshots, dependency order, soft deletion/restoration, and recovery.
- Marked all six generic Common Administration API rows and all four page/middleware/manager/lookup UI rows `DOCUMENTED_VERIFIED`. The agency approval-behalf lookup and administrative dump remain separate domain/operations rows.
- Completed the Applicant-Recipient core-profile slice: corrected the stale claim that NAICS/business identifiers are create-profile fields, added the omitted Registries tab, and documented the actual at-least-one-language validation for legal name, operating name, and description plus agency/subtype matching and exact Team/global visibility.
- Marked seven core Applicant-Recipient profile/list/lookup API rows and eight list/create/edit/general page-component rows `DOCUMENTED_VERIFIED`. All relationship tabs remain open for their dedicated passes.
- Added paired Applicant-Recipient Registries guidance and replaced the paired Agency Financial IDs guidance after tracing their routes, schemas, migrations, mounted tabs, and authorization. The guides now cover registry-specific number formats, the required comment for `other`, global active registry uniqueness, optional agency ownership for financial IDs, active-agency lookup behaviour, exact-parent ownership, soft deletion, and the PostgreSQL `NULL` uniqueness caveat for agency-less financial IDs.
- Marked all four Registry API rows, all five Agency Financial ID API rows, and both mounted tab rows `DOCUMENTED_VERIFIED`; added the Registries destination to both locale sidebars. Other Names, Addresses, and Contacts are the next relationship-tab slice.
- Replaced the paired Other Names, Addresses, and Contacts guides after tracing all 12 handlers, shared schemas, common-row reference checks, migrations, mounted tabs, and error paths. Corrected false field requirements in the old pages; documented exact permissions, list/search behaviour, per-profile other-name uniqueness, Canadian subdivision enforcement, globally unique active contact email, shared address/contact edit refusal, reference-aware soft deletion, and recovery.
- Marked all 12 Other Name/Address/Contact API rows and all three mounted tab rows `DOCUMENTED_VERIFIED` with their dedicated paired destinations.
- Replaced the paired Funding History guide after tracing all eight routes, the five-step wizard, similarity dialog, combined table, matching helpers, money schema, multi-recipient transactions, and database trigger. Corrected stale configured-agency/program and duplicate claims; documented system-row scope masking, current-budget totals by currency, exact versus near matching, confirmation invalidation, access to every affected recipient, unlink semantics, and last-link soft deletion.
- Marked all eight Funding History API rows and all three mounted table/wizard/dialog rows `DOCUMENTED_VERIFIED` with the dedicated paired destination.
- Replaced the paired Proponent Agreements and Reviews guides after tracing independent agreement visibility, relationship navigation, review grouping/actions, generic runtime routes, exact setup scopes, and eligibility. Removed the obsolete planned-intake section; documented that inaccessible agreements are omitted, agreement creation remains scope-checked, review setups apply only by exact proponent or active agreement-linked stream, and runtime configuration is pinned.
- Marked the linked-agreements API row and both mounted Agreements/Reviews tab rows `DOCUMENTED_VERIFIED`. The generic review runtime APIs were already terminal in the Runtime Reviews slice.
- Replaced the paired Proponent Team guides after tracing all five routes, the shared Team helpers/editor, exact-entity capability levels, management ceilings, user lookup, fresh locking, and mounted wrapper. Added the source-backed warning that no last-manager or self-removal guard exists and that recovery requires another authorized manager.
- Marked all five Proponent Team API rows and the mounted Proponent Team wrapper `DOCUMENTED_VERIFIED`. The shared Team editor remains open until its Agreement Team use is reconciled.
- Replaced the paired Agreement Proponents guide after tracing the create-time multi-select, five saved-agreement child routes, independent Agreement/Proponent authorization, exact-parent ownership, locks, soft deletion, table, and migration. Documented the material distinction that creation requires at least one unique proponent but saved-agreement routes have neither a last-link guard nor active agreement-plus-proponent uniqueness.
- Marked all five saved-agreement Proponent API rows plus the mounted tab and create-form selector rows `DOCUMENTED_VERIFIED`.
- Rewrote the paired Funding Agreement overview after tracing the 11 core profile/list/detail/create/update/delete/similarity/lookup handlers, profile schemas and migration, ordered fresh-write path, list/create/detail pages, profile fields and extension slots. Added all 13 core tabs, exact Team capabilities, stream-owned validation, agreement-number uniqueness and matching, target-stream and budget-duration guards, extension vetoes/hooks, and deletion recovery.
- Recorded `DOC-006` as a source-backed documented limitation: near Funding History matches require confirmation fingerprints at the agreement API, but the core create/edit UI has no similarity dialog or confirmation submission path.
- Marked all 11 core Agreement API rows and eight list/create/detail/profile UI rows `DOCUMENTED_VERIFIED`.
- Replaced the paired Agreement Addresses guide after tracing all five handlers, the shared address route helper, localized schemas, migration indexes, and the mounted tab. The guide now covers required riding and main-phone fields, address-type agency ownership, exact Agreement permissions, absent UI controls for API-supported coordinates, shared-address update refusal, reference-aware deletion, and the absence of an active-link uniqueness constraint.
- Marked all five Agreement Address API rows and the mounted address-tab row `DOCUMENTED_VERIFIED`.
- Replaced the paired Agreement Activities guide after tracing the six current-version routes, activity helper, localized schemas, versioning migration, amendment snapshot interaction, mounted tab, lookup fields, and tests. Corrected stale date-search language and documented individually required bilingual fields, lookup action authorization, single-responsible-party auto-selection, transactional link restoration, current working-version isolation, and the precise soft-delete consequences.
- Marked all six current-version Agreement Activity API rows and all three mounted activity tab/lookup-field rows `DOCUMENTED_VERIFIED`. Amendment-specific activity routes remain open for the Amendments pass.
- Added paired Agreement Team guides and sidebar entries after tracing all five routes, canonical exact-agreement scope resolution, shared Team helpers/editor, management ceilings, fresh locking, and active-user lookup. The guides cover level-to-action mapping, role-versus-Team authority, non-disclosing not-found behaviour, cross-entity isolation, soft deletion, and the verified absence of last-manager/self-removal safeguards.
- Marked all five Agreement Team API rows and the shared `Common/EntityTeamTab.vue` row `DOCUMENTED_VERIFIED`; the shared row now has evidence from both Agreement and Proponent mounts.
- Replaced the paired Agreement Budget guide after tracing the nine current-version handlers, budget and stable-lineage helpers, localized schemas, safe-decimal contract, grouped tab, amendment snapshot lineage, migration constraints/triggers, downstream references, and focused tests. Added accurate capacity aggregation across all active agreements in a stream/fiscal year, `numeric(19,2)` and 90-trillion request bounds, current-version/stable-ID behaviour, mixed-currency display semantics, claim/payment deletion guards, and recovery guidance.
- Recorded `DOC-007` as a verified source/documentation discrepancy: the fiscal-year PATCH helper does not repeat the create/lookup duration-overlap guard, block populated/downstream fiscal-year changes, or recalculate existing line capacity for a destination year, despite an older application audit ledger claiming those protections. Both locale guides distinguish the safer UI picker from the API guarantee.
- Marked all nine current-version Agreement Budget API rows and the mounted Budget tab row `DOCUMENTED_VERIFIED`. Amendment-specific budget routes remain open for the Amendments pass.
- Added paired Agreement Amendments guides and sidebar entries after tracing all 22 amendment handlers, amendment/approval helpers, type/subtype and duration schemas, budget/activity snapshot routes, stable lineage, promotion/cancellation/deletion paths, list/detail UI, migration constraints, and managed E2E behaviour. The guide covers single-open-amendment enforcement, capability-specific controls, bilingual names, subtype requirements, snapshot isolation, downstream guards, approval state transitions, promotion, cancellation, and recovery.
- Recorded three verified current limitations: `DOC-008` for the placeholder Recommendation tab with no amendment approval UI, `DOC-009` for missing cross-agreement stream-capacity enforcement in amendment budget writes/promotion, and `DOC-010` because promotion never inserts the defined `Funding_Case_Agreement_Revision` checkpoint row.
- Marked all 22 Agreement Amendment API rows plus the mounted list tab and localized detail page rows `DOCUMENTED_VERIFIED`, closing the amendment-specific budget/activity rows held open in earlier slices.
- Replaced the paired Agreement Commitments guide after tracing all nine commitment overview/root/line/lookup handlers, fresh-write and aggregate-lock helpers, completion and generic approval runtimes, workflow handoff, schemas, PostgreSQL triggers, mounted tab/detail UI, and Outcome Cost Allocation creation replacement. The guide now documents per-commitment current-budget capacity, the stream-commitment-level paid floor, stable coding scope, lifecycle locks, direct completion/activation, soft deletion, and downstream payment eligibility.
- Recorded `DOC-011` because core commitment completion never consults the configured approval template or materializes a routing slip and the detail page mounts no approval controls, despite the generic server approval runtime. Recorded `DOC-012` because the shared money validator and database column permit zero and negative commitment-line amounts.
- Marked all nine Agreement Commitment API rows plus the mounted Commitments tab and localized detail page rows `DOCUMENTED_VERIFIED`.
- Replaced the paired Agreement Forecasts guide after tracing all seven overview/header/line handlers, stable budget lineage, forecast completion and generic approval runtimes, workflow handoff, schemas, migration constraints, and the grouped tab/monthly detail grid. Corrected the former line-status model: lifecycle status belongs to the header, while displayed versions are merely per-line numeric labels.
- Recorded `DOC-013` for the disconnected completion/approval paths and inactive direct-completion result, `DOC-014` for missing inactive-header and monthly-coordinate uniqueness, `DOC-015` for fiscal-year reassignment that can strand invisible old-year lines, and `DOC-016` for sequential non-atomic grid saves plus the server-side negative-amount allowance.
- Marked all seven Agreement Forecast API rows plus the mounted Forecasts tab and localized detail page rows `DOCUMENTED_VERIFIED`.
- Replaced the paired Agreement Payments guide after tracing all 11 overview/header/line/lookup handlers, stable fiscal-year and commitment resolution, remaining-balance locks, completion and separate generic approval runtimes, workflow handoff, schemas, migration triggers/constraints, mounted tab/detail UI, Automated Payments calculation hooks, and Outcome Cost Allocation generated-line guards. Corrected the former claim that completion conditionally enters approval: the core page completes directly and mounts no approval controls.
- Recorded `DOC-017` for the payment completion/approval disconnect, `DOC-018` for the commitment picker/save eligibility mismatch, and `DOC-019` because `pay`, `wait`, `processed`, and `paid` are recognized locked states with no current core or installed-extension writer.
- Marked all 11 Agreement Payment API rows plus the mounted Payments tab and localized detail page rows `DOCUMENTED_VERIFIED`.
- Added paired Agreement Claims and Reconciliation guides and navigation after tracing all 16 overview/header/line/reconciliation/transition handlers, aggregate locks, stable budget lineage, completion and separate approval runtimes, workflow handoff, schemas, migration triggers/constraints, mounted grouped tab/detail workspace, and GC Forms claim materialization/source tab. The guides distinguish submitted claims, reviewer-owned reconciliation records, final-marker semantics, direct completion, and explicit approval integration.
- Recorded `DOC-020` for the reconciliation completion/approval disconnect, `DOC-021` for sequential grid writes and signed/underconstrained financial values, `DOC-022` for fiscal-year group actions targeting the first claim, `DOC-023` for fiscal-year reassignment and duplicate-line gaps, and `DOC-024` for the installed GC Forms README's stale draft-status claim.
- Marked all 16 Agreement Claim/Reconciliation API rows plus the mounted Claims tab and localized detail page rows `DOCUMENTED_VERIFIED`. GC Forms extension inventory rows remain open for the dedicated extension pass.
- Replaced the paired Agreement Monitoring guide after tracing all 25 overview/header/lookup/planning/item/finding/follow-up/update/promising-practice handlers, localized schemas, fresh agreement authorization, aggregate locks, soft-delete cascades, completion and separate approval/workflow runtimes, database constraints, mounted list/detail UI, and monitor extension tabs. The guide now documents insertion-ordered follow-up status, the one-item completion threshold, direct completion, standalone approval boundaries, and complete destructive-action consequences.
- Recorded `DOC-025` because direct monitor completion never consults or materializes the configured approval template and the core workspace cannot create an initial standalone routing slip. Recorded `DOC-026` because the declared edit-status synchronization helper has no executable caller, so child edits do not advance `draft` to `inprogress`.
- Marked all 25 Agreement Monitor API rows plus the mounted Monitors tab and localized detail page rows `DOCUMENTED_VERIFIED`.
- Replaced the paired Agreement Documents and developer Document Generation guides after tracing all five template/list/generate/download/delete handlers, fresh agreement authorization, bilingual context construction, DOCX/HTML/PDF rendering, template trust boundaries, private local storage containment, attachment persistence, soft deletion, cleanup, and the mounted Documents tab. Corrected the former claim that HTML templates can only produce PDF; configured native HTML output is supported.
- Recorded `DOC-027` for hard-coded Health Canada `department.*` context values, `DOC-028` for non-transactional best-effort filesystem cleanup and possible storage drift, and `DOC-029` because the document context-provider array has no current host/extension registrar and is not an extension SDK capability.
- Marked all five Agreement Document API rows plus the mounted Documents tab row `DOCUMENTED_VERIFIED`.
- Expanded the paired Runtime Reviews guide after tracing the remaining four generic Review Set list/create/setup-lookup/cancel handlers, direct target resolvers, exact Applicant-Recipient and Agreement-child authorization, setup-scope and owner-agency eligibility, published snapshot locks, sequential/parallel materialization, blocking-set uniqueness, and terminal cancellation semantics.
- Marked all four remaining Review Set API rows `DOCUMENTED_VERIFIED`; their mounted Applicant-Recipient and runtime-review UI rows were already terminal from the earlier runtime review slice.
- Expanded the paired Common Administration guide after tracing the public allow-listed enum metadata route and the globally authorized cross-agency approval-behalf inventory, including localized query validation, static versus PostgreSQL enum resolution, pagination, literal search, deleted/status precedence, global statistics, middleware exception, and mounted enum consumers.
- Marked the remaining `/api/metadata/enums` and `/api/admin/agency/approval-behalf-types` rows `DOCUMENTED_VERIFIED`.
- Corrected and expanded the paired deployment guide after tracing the public health/readiness handler and globally authorized administrative dump route through database probing, deployment consumers, request abort handling, single-flight worker isolation, timeout/failure protocol, scratch PGlite migration application, pg_dump normalization, response headers, and UI trigger. The guide now makes explicit that the dump is a non-seed core-migration bootstrap, not live application data or a backup.
- Marked `/api/health` and `/api/admin/dump` `DOCUMENTED_VERIFIED`.
- Rewrote the paired Extensions concept guide and expanded the paired host API reference after tracing build discovery and validation, client/server registry generation, agency and stream registry/configuration writes, automatic/manual migrations, lifecycle locks and guards, runtime slots, entity tabs, create actions, payment calculators, dynamic route dispatch, exact entity RBAC, manual-auth obligations, fresh-write protocols, mounted components, full-page configuration, KV/secrets/assets, and the SDK boundary.
- Recorded `DOC-030`: runtime resolvers are consulted only on the agency/Proponent slot path, a false/absent resolution does not suppress the registered slot, and stream slots do not invoke the resolver.
- Marked all ten host extension API rows, the extension configuration page, six extension host components, and the host-platform extension inventory row `DOCUMENTED_VERIFIED`. Installed-extension implementation rows remain open for direct submodule verification.
- Rewrote the paired Automated Payments guide after tracing its manifest, stream activation guard/configuration, sole payment-calculator contribution, exact agreement-update calculation route, financial queries and formulas, two-decimal rounding, current-budget identities, holdback release and KV provenance, transactional before/after-create hooks, localized errors, and recovery. Clarified that it contributes no separate agreement tab or replacement create action.
- Recorded `DOC-031`: a payment type disabled in stream config returns a zero disabled result that the current calculator still publishes and the host can copy into Amount, while the server correctly skips extension ceiling enforcement for that type.
- Marked all 12 Automated Payments extension inventory rows `DOCUMENTED_VERIFIED`.
- Expanded the paired GC Forms Integration guide after tracing all 15 handlers, credential UI and encrypted-secret writes, stream/modal/page configuration, templates and claim-template generation, preview, submission listing, sync and confirmation reconciliation, manual materialization recovery, entity-source tabs, runtime/versioning helpers, external client crypto/timeouts, lifecycle guards, both migrations, and claims-first materialization. Added route/permission, external-service security, data-integrity, presentation, backup, and failure guidance.
- Recorded `DOC-032`: the preview handler's manifest declares stream read, but the extension-level authorization requires stream update; documented the effective permission while preserving that preview itself is non-persistent.
- Marked all 36 GC Forms Integration extension inventory rows `DOCUMENTED_VERIFIED`.
- Expanded the paired Narrative Quality guide after tracing stream normalization/configuration, the assessment-target handler and host dispatcher, exact agreement/assessment/comment target contexts, config normalization and legacy compatibility, browser worker queue/model lifecycle, public worker/WASM/model assets, scoring/refinement/presentation rules, privacy, error/reset behavior, and absent persistence/hooks.
- Recorded `DOC-033`: the extension README/direct-handler test says assessment-target discovery needs no enablement row, but the externally reachable host dispatcher requires both agency and stream enablement.
- Marked all 12 Narrative Quality extension inventory rows `DOCUMENTED_VERIFIED`.
- Expanded the paired Narrative Tags guide after tracing its manifest/capabilities, independent target configuration, agreement and Proponent slots, host runtime resolver behaviour, source-aware read/write routes, fresh-authorization transaction writes and lifecycle hooks, KV ownership/provenance, browser worker/model assets, keyword fallback, and failure states.
- Recorded `DOC-034`: the Proponent resolver computes lead-agency and readable linked-stream sources, but the host agency runtime path skips extension resolvers unless the lead agency has enabled the extension and still emits a registered slot when a resolver returns `null`; the mounted-slot behaviour therefore does not implement the documented “lead agency or linked agreement” rule even though the Proponent GET route filters sources correctly.
- Marked all 20 Narrative Tags extension inventory rows `DOCUMENTED_VERIFIED`.
- Expanded the paired Outcome Cost Allocation guide after tracing all declared host capabilities, stream mapping configuration, the update-only agreement tab and replacement commitment action, all five dynamic handlers and exact RBAC, fresh-authorization writes, allocation versioning and exact decimal/cent math, commitment and payment create hooks, lifecycle/disable guards, three ordered migrations, database triggers, immutable provenance, PGlite/PostgreSQL boundaries, and deployment/backup requirements.
- Marked all 25 Outcome Cost Allocation extension inventory rows `DOCUMENTED_VERIFIED`; every row in `extension-coverage.json` is now terminal.
- Reconciled all ten ordered production migrations and all material entity/integrity rows against the migration registry, canonical schema manifest, generated database types, architecture contracts, and executable tests. Expanded the paired data-model reference with explicit `0001`–`0010` ownership, constraint, trigger/function, precision, soft-delete, polymorphic, agreement-financial, and extension-storage contracts.
- Marked all 212 data inventory rows `DOCUMENTED_VERIFIED`; every row in `data-coverage.json` is now terminal.
- Closed the remaining shared UI sweep after tracing 34 assessment/checklist/review-schema and common-control components to their source-wide consumers. Expanded the paired navigation/shared-control and checklist result-rule guidance; marked 31 mounted rows `DOCUMENTED_VERIFIED` and three source-wide unmounted components `NOT_APPLICABLE_VERIFIED`.
- Every one of the 291 rows in `page-coverage.json` is now terminal (281 documented and 10 verified not applicable).
- Reconciled all ten configuration/deployment surfaces and all 102 historical audit, investigation, and browser-persona records against current authoritative source and tests. Expanded the paired operator configuration/deployment and developer audit-impact references, corrected the unsupported `RAILWAY_RUN_UID` workaround, and remapped three stale historical evidence paths.
- Marked every configuration and audit-impact row `DOCUMENTED_VERIFIED` and closed all 16 umbrella domain rows after confirming their detailed page, API, data, extension, configuration, and audit-impact inventories were terminal.
- Closed `DOC-001` through `DOC-005`; all 34 documentation findings now have source-backed bilingual dispositions. Recorded application discrepancies remain documented current behaviour and were not patched.
- Historical audit evidence and remaining source/docs files are being reconciled; no row is terminal merely because it was generated.

## Inventory state

Run `bun run docs:inventory` after source drift. Generated rows preserve prior manual dispositions by stable ID. Every row currently uses `IN_PROGRESS` until its evidence and both locale destinations are directly verified.

## Validation results

- `bun run docs:inventory` — passed; source counts above reconciled.
- `bun run docs:build` — passed after the first operator/API batch.
- `VITEPRESS_BASE=/gcs-ssc-docs/ bun run docs:build` — passed when run sequentially. An earlier concurrent invocation collided in VitePress's shared `.temp` directory; it was not a content failure.
- `git diff --check` — passed.
- `bun run docs:check` — intentionally fails; outstanding errors fell from 2,681 to 1,372 after missing API/operator/data destinations were added. The gate rejects non-terminal rows and now compares English/French heading, table, and code-block structure.
- Strengthened checker diagnostic ordering, repaired the only detected locale-structure mismatch and dead heading anchor, and regenerated destination mappings by component domain. The latest checker has only expected non-terminal rows plus the required final zero-items status assertion.
- Identity API/helper focused tests — 64/64 passed across seven files.
- Identity page/request tests — 25/25 passed; role modal-state tests — 7/7 passed.
- Shell/shared-component tests — 42/42 passed across layout, dashboard state, CRUD accessibility/state, confirmation, and saved lookup hydration.
- Agency route/schema/helper/page/component tests — 188/188 passed across 21 files. Test-stub warnings for unresolved shared Vue components were non-failing and did not affect the assertions.
- Transfer-payment program profile/wizard/budget/outcome/objective/indicator tests — 161/161 passed across 22 files.
- Transfer-payment core stream route/schema/composable/component tests — 144/144 passed across 30 files (127 tests in the primary slice plus 17 lifecycle/smoke/source-inventory tests). Expected component-stub warnings and assessment-normalization smoke diagnostics were non-failing.
- Review setup, assessment set, assessment schema, versioning, editor, and component tests — 135/135 passed across 37 files. Expected component-stub warnings were non-failing.
- Checklist schema/editor/evaluation/versioning tests — 41/41 passed across nine files. Expected component-stub warnings were non-failing.
- Recommendation schema/setup/publication/runtime-regression/component tests — 43/43 passed across 11 files.
- Approval-template route/scope/schema/publication/editor/component tests — 47/47 passed across 11 files.
- Document-template multipart/schema/storage/routes/UI and generation-tool/security tests — 61/61 passed across seven files. Expected component-stub warnings were non-failing.
- Workflow setup/schema/runtime/route/UI tests — 52/52 passed across six files.
- `bun run docs:build` — passed after the paired workflow guide and navigation were added.
- Latest `bun run docs:check` — intentionally fails with 784 expected remaining-item errors only; no locale-structure, navigation, link, anchor, redirect, source-path, or discovery error was reported.
- Runtime assessment/checklist/additional-reviewer/clone/access tests — 109 passed across 17 files; three additional source-suite cases were skipped by their existing test definitions.
- `bun run docs:build` — passed after the paired runtime-review guide and navigation were added.
- Latest `bun run docs:check` — intentionally fails with 761 expected remaining-item errors only; no locale-structure, navigation, link, anchor, redirect, source-path, or discovery error was reported.
- Approval/completion adapter, route, lifecycle, persistence, locking, schema, and component tests — 84/84 passed across 18 files.
- `VITEPRESS_BASE=/gcs-ssc-docs/ bun run docs:build` — passed after the approval/completion rewrite.
- `bun run docs:build` — passed when rerun alone after an earlier concurrent normal/alternate-base invocation collided in VitePress's shared output directory; the collision was not a content failure.
- Latest `bun run docs:check` — intentionally fails with 745 expected remaining-item errors only; no locale-structure, navigation, link, anchor, redirect, source-path, or discovery error was reported.
- Shared recommendation/review presentation tests — 5/5 passed across three files; one unresolved-component warning in the shallow review-schema test was non-failing.
- Common Administration configuration/schema/route/transaction/lookup/component/i18n tests — 153/153 passed across 11 files. Expected shallow-component stub warnings were non-failing.
- Latest `bun run docs:check` — intentionally fails with 732 expected remaining-item errors only; no locale-structure, navigation, link, anchor, redirect, source-path, or discovery error was reported.
- `bun run docs:build` — passed after the Common Administration rewrite.
- Applicant-Recipient core profile/schema/route/layout/field tests — 56/56 passed across four files.
- Latest `bun run docs:check` — intentionally fails with 717 expected remaining-item errors only; no structural error was reported. `git diff --check` passes.
- Applicant-Recipient Registry/Agency Financial ID route, schema, child-tab, and detail-state tests — 85/85 passed across five files.
- Latest `bun run docs:check` after the Registry/Agency Financial ID slice — intentionally failed with 706 expected remaining-item errors only; both normal-root and `/gcs-ssc-docs/` production builds passed sequentially, and `git diff --check` passed.
- Applicant-Recipient Other Name/Address/Contact route, schema, uniqueness, shared-row, child-tab, and detail-state tests — 106/106 passed across seven files.
- Latest `bun run docs:check` — intentionally fails with 691 expected remaining-item errors only; no locale-structure, navigation, link, anchor, redirect, source-path, discovery, or ledger-schema error was reported.
- Both normal-root and `/gcs-ssc-docs/` production builds passed sequentially after the Other Name/Address/Contact rewrite; `git diff --check` passed.
- Applicant-Recipient Funding History routes, matching, schema, migration/trigger, safe-decimal, detail-state, and UI tests — 41/41 passed across eight files.
- Latest `bun run docs:check` — intentionally fails with 680 expected remaining-item errors only; no structural error was reported.
- Both normal-root and `/gcs-ssc-docs/` production builds passed sequentially after the Funding History rewrite; `git diff --check` passed.
- Applicant-Recipient Agreements/Reviews relationship-route, mounted-tab, and runtime-access tests — 67/67 passed across four discovered files.
- Latest `bun run docs:check` — intentionally fails with 677 expected remaining-item errors only; two initially stale cross-links were corrected and no structural error remains.
- Proponent Team route, wrapper/editor, detail-layout, and server-route smoke tests — 22/22 passed across five files. Expected assessment-normalization diagnostics in the broad smoke suite were non-failing.
- Latest `bun run docs:check` — intentionally fails with 671 expected remaining-item errors only; no structural error was reported.
- Both normal-root and `/gcs-ssc-docs/` production builds passed sequentially after the Proponent Team rewrite; `git diff --check` passed.
- Agreement Proponent route, lookup, selector hydration, and child-tab tests — 127/127 passed across four files. Expected shallow-component warnings from sibling agreement tabs were non-failing.
- Latest `bun run docs:check` — intentionally fails with 664 expected remaining-item errors only; no structural error was reported.
- Core Agreement route, schema, utility, transactional deletion, profile form/display, and detail-layout tests — 131/131 passed across eight files. Expected shallow-component warnings were non-failing.
- Latest `bun run docs:check` — intentionally fails with 645 expected remaining-item errors only; no structural error was reported.
- Agreement Address route, schema, shared-row, and child-tab tests — 152/152 passed across three files. Expected shallow-component warnings from sibling Agreement tabs were non-failing.
- Latest `bun run docs:check` after the Agreement Addresses slice — intentionally fails with 639 expected remaining-item errors only; no locale-structure, navigation, link, anchor, redirect, source-path, discovery, or ledger-schema error was reported.
- Both normal-root and `/gcs-ssc-docs/` production builds passed sequentially after the Agreement Addresses rewrite; `git diff --check` passed.
- Agreement Activity route, schema, amendment snapshot, child-tab, and lookup-field tests — 166/166 passed across six files. Expected shallow-component warnings from sibling Agreement tabs were non-failing.
- Latest `bun run docs:check` after the Agreement Activities slice — intentionally fails with 630 expected remaining-item errors only; an initially dead Amendments-page link was corrected and no structural error remains.
- Both normal-root and `/gcs-ssc-docs/` production builds passed sequentially after the Agreement Activities rewrite; `git diff --check` passed.
- Agreement Team route authorization/inventory, shared editor, and agreement-route tests — 48/48 passed across four discovered files; one requested component test path did not exist and was not discovered by Vitest.
- Latest `bun run docs:check` after the Agreement Team slice — intentionally fails with 624 expected remaining-item errors only; no structural error was reported.
- Both normal-root and `/gcs-ssc-docs/` production builds passed sequentially after adding the Agreement Team guide/navigation; `git diff --check` passed.
- Agreement Budget route, schema, duration, grouped-tab, amendment lineage/runtime, and migration tests — 217/217 passed across nine files. Expected shallow-component warnings from sibling Agreement tabs were non-failing. The opt-in PostgreSQL budget-version suite was not run because `AGREEMENT_CONCURRENCY_POSTGRES_TEST_URL` is not configured; final managed PostgreSQL verification remains outstanding.
- Latest `bun run docs:check` after the Agreement Budget slice — intentionally fails with 614 expected remaining-item errors only; no locale-structure, navigation, link, anchor, redirect, source-path, discovery, or ledger-schema error was reported.
- Both normal-root and `/gcs-ssc-docs/` production builds passed sequentially after the Agreement Budget rewrite; `git diff --check` passed.
- Agreement Amendment route, approval runtime, snapshot/runtime, deletion, scope, UI, schema, and migration tests — 117/117 passed across 11 files. Expected shallow-component warnings from sibling Agreement tabs were non-failing.
- Managed `bun run test:e2e:light:spec -- tests/e2e/agreement-amendments.spec.ts` — 3/3 passed, including duration snapshot controls, stable financial identity/discard behaviour, subtype/bilingual edits, cancellation, and replacement draft creation. The AGENTS-documented `test:e2e:fast:spec` alias is absent from the current manifest; its attempted invocation ran no tests, so the current managed single-spec alias was used.
- Latest `bun run docs:check` after the Agreement Amendments slice — intentionally fails with 590 expected remaining-item errors only; two initially stale approval-guide links were corrected and no structural error remains.
- Both normal-root and `/gcs-ssc-docs/` production builds passed sequentially after adding the Agreement Amendments guide/navigation; `git diff --check` passed.
- Agreement Commitment route, locking, deletion atomicity, schema/money, completion, approval-runtime, mounted-tab, and detail-page tests — 198/198 passed across 15 files. Expected unresolved-component warnings in shallow UI suites and the Vue Suspense notice were non-failing.
- Latest `bun run docs:check` after the Agreement Commitments slice — intentionally fails with 579 expected remaining-item errors only; no locale-structure, navigation, link, anchor, redirect, source-path, discovery, or ledger-schema error was reported.
- Both normal-root and `/gcs-ssc-docs/` production builds passed sequentially after the Agreement Commitments rewrite; `git diff --check` passed.
- Agreement Forecast route, stable-line validation, completion, generic approval, migration, schema, grouped-tab, and detail-grid tests — 208/208 passed across 12 files. Expected unresolved-component warnings in shallow UI suites were non-failing.
- Latest `bun run docs:check` after the Agreement Forecasts slice — intentionally fails with 570 expected remaining-item errors only; no locale-structure, navigation, link, anchor, redirect, source-path, discovery, or ledger-schema error was reported.
- Both normal-root and `/gcs-ssc-docs/` production builds passed sequentially after the Agreement Forecasts rewrite; `git diff --check` passed.
- Agreement Payment route, schema, fiscal-year lookup, completion, generic approval, mounted-tab, and detail-page host tests — 167/167 passed across seven files. Expected unresolved-component warnings in shallow UI suites were non-failing.
- Automated Payments focused extension tests — 32/32 passed across six files in the owning extension workspace. Outcome Cost Allocation payment/create/guard tests — 94/94 passed across four files in its owning workspace.
- Latest `bun run docs:check` after the Agreement Payments slice — intentionally fails with 557 expected remaining-item errors only; two initially dead workflow links were corrected and no structural error remains.
- Both normal-root and `/gcs-ssc-docs/` production builds passed sequentially after the Agreement Payments rewrite; `git diff --check` passed.
- Agreement Claim route/helper/schema, transition, completion, generic approval, grouped-tab, and detail-workspace tests — 126/126 passed across nine host files. Expected unresolved-component warnings in shallow UI suites were non-failing.
- GC Forms claim materialization, manual recovery, failure-list, source-tab, and submission-preflight extension tests — 35/35 passed across five files in the owning extension workspace.
- Latest `bun run docs:check` after the Agreement Claims slice — intentionally fails with 539 expected remaining-item errors only; no locale-structure, navigation, link, anchor, redirect, source-path, discovery, or ledger-schema error was reported.
- Both normal-root and `/gcs-ssc-docs/` production builds passed sequentially after the Agreement Claims rewrite; duplicate sidebar keys exposed by the first successful build were removed and the clean build was rerun. `git diff --check` passed.
- Agreement Monitor schema, route, aggregate-lock/delete, follow-up derivation, completion, approval materialization/RBAC, runtime adapter, lookup, modal lifecycle, and detail-workspace tests — 136/136 passed across 17 files.
- Latest `bun run docs:check` after the Agreement Monitors slice — intentionally fails with 512 expected remaining-item errors only; two initially dead workflow links were corrected, and no locale-structure, navigation, link, anchor, redirect, source-path, discovery, or ledger-schema error was reported.
- Both normal-root and `/gcs-ssc-docs/` production builds passed sequentially after the Agreement Monitoring rewrite; the alternate-base build emitted only the existing non-failing bundle-size warning. `git diff --check` passed, both scoped ledger queries returned zero non-terminal monitor rows, and the application worktree remained clean.
- Agreement Document routes, tab, rendering security/tool resolution, template validation/storage, local-file containment, and stream-template tests — 97 passed across 10 files; one platform-specific storage case was skipped by its source definition. Expected unresolved-component warnings in the shallow stream-template tab suite were non-failing.
- Latest `bun run docs:check` after the Agreement Documents slice — intentionally fails with 506 expected remaining-item errors only; no locale-structure, navigation, link, anchor, redirect, source-path, discovery, or ledger-schema error was reported.
- Both normal-root and `/gcs-ssc-docs/` production builds passed sequentially after the Agreement Documents rewrite once literal HTML-template braces were entity-escaped for VitePress; both builds emitted only the existing non-failing bundle-size warning. `git diff --check` passed, both scoped ledger queries returned zero non-terminal document rows, and the application worktree remained clean.
- Review Set collection/access/runtime tests — 50 passed across five files; three helper cases were skipped by their source definitions.
- Latest `bun run docs:check` after the remaining Review Set route slice — intentionally fails with 502 expected remaining-item errors only; no structural error was reported.
- Both normal-root and `/gcs-ssc-docs/` production builds passed sequentially after the Runtime Reviews expansion with only the existing non-failing bundle-size warning. `git diff --check` passed and the application worktree remained clean.
- Shared metadata enum, global approval-behalf, API-auth middleware, and enum-consumer tests — 21/21 passed across four files.
- Latest `bun run docs:check` after the shared metadata lookup slice — intentionally fails with 500 expected remaining-item errors only; no structural error was reported.
- Both normal-root and `/gcs-ssc-docs/` production builds passed sequentially after the shared lookup expansion with only the existing non-failing bundle-size warning. `git diff --check` passed and the application worktree remained clean.
- Health/readiness, Railway/Docker consumer, administrative dump route/helper/generator, and default-layout trigger tests — 32/32 passed across six files.
- Latest `bun run docs:check` after the operator-route slice — intentionally fails with 498 expected remaining-item errors only; no structural error was reported.
- Both normal-root and `/gcs-ssc-docs/` production builds passed sequentially after the operator-route correction with only the existing non-failing bundle-size warning. `git diff --check` passed and the application worktree remained clean.
- Host extension registry, route, runtime, SDK-boundary, UI, migration, and secret-store tests — 117/117 passed across 16 host files. Public SDK workspace tests — 19/19 passed across three files. The Browserslist-age notice during the production-registry test was non-failing.
- Latest `bun run docs:check` after the host extension slice — intentionally fails with 481 expected remaining-item errors only; no locale-structure, navigation, link, anchor, redirect, source-path, discovery, or ledger-schema error was reported. Closing the host-platform extension inventory row reduces the expected next run by one further item.
- Both normal-root and `/gcs-ssc-docs/` production builds passed sequentially after the host extension rewrite with only the existing non-failing bundle-size warning. `git diff --check` passed and the application worktree remained clean.
- Automated Payments owning unit suite — 32/32 passed across six files; owning `vue-tsc --noEmit` typecheck passed.
- Latest `bun run docs:check` after the Automated Payments slice — intentionally fails with 468 expected remaining-item errors only; no structural or bilingual-parity error was reported.
- Both normal-root and `/gcs-ssc-docs/` production builds passed sequentially after the Automated Payments rewrite with only the existing non-failing bundle-size warning. `git diff --check` passed, the scoped extension query returned zero non-terminal rows, and the application worktree remained clean.
- GC Forms Integration owning unit suite — 118/118 passed across 17 files; owning `vue-tsc --noEmit` typecheck passed. Its two PostgreSQL lifecycle/materialization integration suites were not run because neither `POSTGRES_TEST_URL` nor `DATABASE_URL` is configured; the final managed PostgreSQL gate remains outstanding.
- Latest `bun run docs:check` after the GC Forms slice — intentionally fails with 432 expected remaining-item errors only; no structural or bilingual-parity error was reported.
- Both normal-root and `/gcs-ssc-docs/` production builds passed sequentially after the GC Forms expansion with only the existing non-failing bundle-size warning. `git diff --check` passed, the scoped extension query returned zero non-terminal rows, and the application worktree remained clean.
- Narrative Quality owning unit suite — 57/57 passed across eight files; owning `vue-tsc --noEmit` and Vue language-server check (zero diagnostics across six checked files) passed. The Vue Suspense notice in the component suite was non-failing.
- Latest `bun run docs:check` after the Narrative Quality slice — intentionally fails with 420 expected remaining-item errors only; no structural or bilingual-parity error was reported.
- Both normal-root and `/gcs-ssc-docs/` production builds passed sequentially after the Narrative Quality expansion with only the existing non-failing bundle-size warning. `git diff --check` passed, the scoped extension query returned zero non-terminal rows, and the application worktree remained clean.
- Narrative Tags owning unit suite — 48/48 passed across five files; owning `vue-tsc --noEmit` typecheck passed.
- Latest `bun run docs:check` after the Narrative Tags slice — intentionally fails with 400 expected remaining-item errors only; no structural or bilingual-parity error was reported.
- Both normal-root and `/gcs-ssc-docs/` production builds passed sequentially after the Narrative Tags expansion with only the existing non-failing bundle-size warning. `git diff --check` passed, the scoped extension query returned zero non-terminal rows, and the application worktree remained clean.
- Outcome Cost Allocation owning unit suite — 183/183 passed across 13 files; owning `vue-tsc --noEmit` typecheck passed. The Vue Suspense notice was non-failing. Its opt-in PostgreSQL concurrency suite was not run because `OUTCOME_ALLOCATION_POSTGRES_TEST_URL` is not configured; the final managed PostgreSQL gate remains outstanding.
- Latest `bun run docs:check` after the Outcome Cost Allocation slice — intentionally fails with 375 expected remaining-item errors only; all extension inventory rows are terminal and no structural or bilingual-parity error was reported.
- Both normal-root and `/gcs-ssc-docs/` production builds passed sequentially after the Outcome Cost Allocation expansion with only the existing non-failing bundle-size warning. `git diff --check` passed and the application worktree remained clean.
- Data canonical-schema, migration-regression, approval-enforcement, agreement-revision, agreement-amendment, and spreadsheet-reconciliation tests — 71/71 passed across six files.
- Both normal-root and `/gcs-ssc-docs/` production builds passed sequentially after the data-model expansion; an initial concurrent build collision in VitePress's shared temporary output was resolved by the sequential rerun. `git diff --check` passed, all data rows were terminal, and the application worktree remained clean.
- Shared assessment/checklist/review-schema and common UI component tests — 290/290 passed across 12 files. Expected shallow-component and Vue Suspense warnings were non-failing. `git diff --check` passed, all page rows were terminal, and the application worktree remained clean.
- `bun install --frozen-lockfile` in the documentation repository — passed with no dependency changes (the execution environment provided Bun 1.3.10 while the project metadata targets Bun 1.3.13).
- Configuration/audit-impact integration validation — all 10 configuration and 102 audit-impact rows terminal; both production build bases and `git diff --check` passed, and the application worktree remained clean.
- The final pre-integration `bun run docs:check` intentionally reported only 17 integration-owned items (16 umbrella domain statuses and the zero-remaining status assertion); all detailed inventories and structural checks were clean. Those 17 items were subsequently reconciled before the passing final run below.
- Final `bun run docs:check` — passed, including production build, English/French parity, navigation/sidebar targets, links/anchors, redirects, duplicate routes, ledger schema/statuses, source paths, and discovery reconciliation for every current page/component, API handler, registered migration, and installed extension.
- Final explicit `bun run docs:build` and `VITEPRESS_BASE=/gcs-ssc-docs/ bun run docs:build` — passed sequentially with only the existing non-failing bundle-size warning. Final `git diff --check` passed.
- Every coverage ledger reports zero non-terminal rows: 16 domains, 291 pages/layouts/middleware/components, 428 API handlers, 212 data/migration mechanisms, 106 extension entries, 10 configuration entries, and 102 historical audit-impact records.
- Final `NODE_OPTIONS=--max-old-space-size=8192 bun run quality:whole` — passed every configured stage: Fallow/dependency reports, ESLint/oxlint (warnings only), application typecheck, public SDK build/typecheck, all five extension typechecks, production artifact/portable database smoke, 3,240 unit tests with four source-defined skips across 446 files, and coverage (87.95% statements, 81.10% branches, 85.69% functions, 89.35% lines). Static Fallow and oxlint findings remain advisory reports, consistent with repository policy; there were no errors or failed stages.
- External PostgreSQL-only suites were unavailable because `POSTGRES_TEST_URL`, `DATABASE_URL`, `AGREEMENT_CONCURRENCY_POSTGRES_TEST_URL`, and `OUTCOME_ALLOCATION_POSTGRES_TEST_URL` were not configured. This is an environment limitation, not an application failure; focused PGlite/migration suites and the production artifact database smoke passed.
- Final source-drift check: application `main` remains at `debd15e3600e9c140b23756d849dc40964a27910`, its worktree is clean, every recorded extension/SDK gitlink is unchanged, and the documentation repository remains on its original `main` HEAD with the synchronization intentionally uncommitted. No row required reopening.
- Completion-audit regeneration: corrected `scripts/generate-documentation-inventory.ts` so source rediscovery preserves manually verified audiences, bilingual destinations, evidence, statuses, and notes. `bun run docs:inventory` then regenerated all seven ledgers from the final application checkout with exactly 291 page/shell/component, 428 API, 212 data, 106 extension, 16 domain, 10 configuration, and 102 audit-impact rows. Pre/post canonical hashes of every curated field matched, and every regenerated ledger retained zero non-terminal rows.
- Post-regeneration final rerun: `bun install --frozen-lockfile`, `bun run docs:check`, `bun run docs:build`, `VITEPRESS_BASE=/gcs-ssc-docs/ bun run docs:build`, and `git diff --check` all passed sequentially. The builds emitted only the existing non-failing bundle-size warning.
- Post-regeneration `NODE_OPTIONS=--max-old-space-size=8192 bun run quality:whole` also passed all 15 configured stages with none failed or skipped; its regenerated summary is timestamped `2026-08-17T04:17:24Z`. The final application HEAD/worktree and all six gitlinks remained unchanged and clean afterward.

## Blockers

None.

## Exact next action

None. The synchronization is complete; no commit or publication was requested.
