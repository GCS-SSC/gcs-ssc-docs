# Documentation findings

| ID | Category | Evidence | Impact | Remediation | Status |
| --- | --- | --- | --- | --- | --- |
| DOC-001 | Missing audit controls | Documentation baseline has no `documentation-audit/` directory or coverage ledgers. | Completion cannot be mechanically demonstrated. | Add durable inventories, status, baseline, findings, and verification. | DOCUMENTED_VERIFIED |
| DOC-002 | Missing verification command | Baseline `package.json` exposes only VitePress dev/build/preview scripts. | Locale parity, links, anchors, redirects, source coverage, and terminal status are unchecked. | Implement `bun run docs:check`. | DOCUMENTED_VERIFIED |
| DOC-003 | Audience coverage gap | Existing navigation has user/admin/concept/developer material but no clearly identified operator section. | Deployment, backup, monitoring, security, and troubleshooting guidance is incomplete. | Add bilingual operator documentation and navigation. | DOCUMENTED_VERIFIED |
| DOC-004 | API reference scope | The pinned target registers 557 API handlers; grouped references are generated from `server/api`. | The surface now also includes Agency catalog, group, note, and claim routes. | Keep all ten paired API references and their source-backed coverage ledger aligned with the pinned source. | DOCUMENTED_VERIFIED |
| DOC-005 | Data reference gap | Existing site has no exhaustive migration/entity/integrity reference. | Constraints, triggers, precision, polymorphism, concurrency, and PostgreSQL/PGlite differences are incomplete. | Add bilingual data-model references and material mechanism inventory rows. | DOCUMENTED_VERIFIED |
| DOC-006 | Resolved: Agreement similarity confirmation | Agreement create/profile components now use the funding-history similarity warning and submit value-bound confirmations. | Near matches can be reviewed and confirmed through the core UI; exact conflicts remain blocked. | The paired Agreement overview guides describe confirmation, invalidation after edits, and retry. | DOCUMENTED_VERIFIED |
| DOC-007 | Resolved: Agreement budget fiscal-year mutation | `server/utils/agreement-budget.ts` validates duration overlap and protects referenced fiscal-year identities during current-budget writes. | The former unrestricted reassignment warning no longer describes the target. | Document current lookup, reference guards, and stable identities in both Budget guides. | DOCUMENTED_VERIFIED |
| DOC-008 | Resolved: Amendment approval UI | The Amendment detail page mounts completion, workflows, and the immutable approval packet. Required completion dispatches its configured approval-submission workflow. | Approval is an executable core UI path. | Both Amendment guides describe prerequisites, snapshots, decisions, cancellation, and promotion. | DOCUMENTED_VERIFIED |
| DOC-009 | Amendment budget capacity gap | Amendment budget-line create/patch handlers validate the draft/type/version, cost-line ownership, and shared money schema but do not call `assertAgreementBudgetProgramFundingCapacity`; `promoteApprovedAgreementAmendment` also lacks the cross-agreement stream-capacity check used by current-budget writes. | A draft snapshot and its promotion can exceed the stream budget plus overcommit threshold even though equivalent edits in the ordinary Budget tab are rejected. | Warn both audiences that successful amendment snapshot persistence is not capacity evidence and require manual capacity reconciliation before approval. | DOCUMENTED_VERIFIED |
| DOC-010 | Resolved: Agreement revision writer | `agreement-approval-submission.ts` and `workflow-runtime.ts` capture immutable packets and promote hash-verified successful approval to Agreement revision 0 or the next amendment revision. | Revision evidence represents an executable approval checkpoint. | Describe packet domains, immutability, hashes, and revision promotion in both languages. | DOCUMENTED_VERIFIED |
| DOC-011 | Resolved: Commitment completion approval | The entity registry and shared completion transition select optional `approval_submission` with `on_completion`; `completion-positive-terminus.ts` activates the successful Commitment and supersedes the prior active one of the same type. | Completion and approval success are separate; no applicable workflow produces immediate positive effects. | The paired Commitment and completion guides describe the supported core path. | DOCUMENTED_VERIFIED |
| DOC-012 | Commitment amount sign validation gap | `FundingCaseAgreementCommitmentLineBaseSchema` uses `RequiredMoney()`, not `RequiredPositiveMoney()`, and the `numeric(19,2)` column has no non-negative check constraint. The UI amount control also has no minimum. | Zero or negative commitment lines can be saved even though they ordinarily represent a positive financial commitment, and can distort displayed totals and capacity checks. | State the exact accepted numeric contract and warn operators to apply agency review controls rather than claiming the screen enforces positive amounts. | DOCUMENTED_VERIFIED |
| DOC-013 | Resolved: Forecast completion approval | The shared completion transition starts the optional approval-submission workflow; positive termination activates the Forecast and supersedes the prior active header for the same Agreement/fiscal-year identity. | The previous direct-complete/inactive-only claim is obsolete. | Both Forecast guides distinguish header completion, line versions, and activation. | DOCUMENTED_VERIFIED |
| DOC-014 | Forecast header/version uniqueness gap | The migration has only a partial unique index for an *active* agreement/fiscal-year header and no unique constraint on forecast-line `(forecast, budget line, month, version)`. The UI's grouped actions select the first header and its detail cell map overwrites duplicates. | Multiple inactive headers or duplicate monthly coordinates can be created through the API and then be represented ambiguously or incompletely by the core UI. | Document that versions are line labels rather than entities and require one header and one line per logical coordinate as an operational invariant. | DOCUMENTED_VERIFIED |
| DOC-015 | Resolved: Forecast fiscal-year reassignment | `server/utils/agreement-forecast.ts` rejects a header fiscal-year change while non-deleted lines exist. | The old hidden-line reassignment path is blocked. | Document the guard and choosing the fiscal year before data entry. | DOCUMENTED_VERIFIED |
| DOC-016 | Forecast grid non-atomicity and amount-sign gap | The page saves up to 12 cells per current budget line as sequential individual POST/PATCH requests. The UI input has `min=0`, but the route schema uses `RequiredMoney()` and the database has no non-negative check. | A late request failure can leave earlier cells saved, and a direct API client can persist negative forecast amounts despite the UI affordance. | Document partial-save recovery and distinguish the UI minimum from the server/database contract. | DOCUMENTED_VERIFIED |
| DOC-017 | Resolved: Payment completion approval | Payment completion delegates to the shared transition and optional approval-submission workflow; the detail page exposes workflow progress. | The old standalone-API-only approval limitation no longer applies. | Describe completion evidence, approval configuration, and recovery in both Payment guides. | DOCUMENTED_VERIFIED |
| DOC-018 | Payment commitment lookup eligibility mismatch | The Payment commitment lookup can include completed inactive Commitments. `resolveActiveAgreementPaymentCommitmentByType` requires the active Commitment of that type plus completion or latest approved routing evidence. | Picker presence alone does not establish save eligibility; resolution uses the active Commitment of the chosen type. | Retain the eligibility distinction and explain exact-line payment coverage in both Payment and Commitment guides. | DOCUMENTED_VERIFIED |
| DOC-019 | Resolved: Obsolete Payment state model | Agency-owned `Common_Status` IDs replace the old payment enum. Workflow transitions use configured statuses; the host does not infer disbursement from a status name. | Old `pay`, `wait`, `processed`, and `paid` enum guidance would misdescribe the target. | Describe the Agency status model and avoid promising automated funds movement. | DOCUMENTED_VERIFIED |
| DOC-020 | Resolved: Claim reconciliation approval | Claim reconciliation completion uses the shared optional approval-submission transition. Positive termination closes the reconciliation and can apply the Agency final-reconciliation status to the Claim. | The former completion/approval disconnect is removed. | Both Claims guides explain finality, optional approval, and the separate Closeout evidence requirement. | DOCUMENTED_VERIFIED |
| DOC-021 | Claim submission partial saves and signed amounts | Submission cells still save sequentially. Reconciliation uses an atomic bulk route with the exact current claim-line set. Shared money schemas accept signed amounts and do not impose every business inequality. | A submission error can leave earlier cells saved; reconciliation no longer has the same partial-grid behavior. Signed or internally inconsistent business amounts still require review. | Document the two save contracts separately and retain explicit financial review guidance. | DOCUMENTED_VERIFIED |
| DOC-022 | Resolved: Claim row action ambiguity | `AgreementClaimsTab.vue` exposes edit/delete on individual claim leaves. | The old first-row group action warning no longer applies. | Describe individual Claim actions and fiscal-year grouping without that warning. | DOCUMENTED_VERIFIED |
| DOC-023 | Claim duplicate-line gap; fiscal-year guard resolved | Claim PATCH rejects fiscal-year changes that conflict with active allocated lines. No active `(claim, budget line)` uniqueness constraint prevents duplicate logical allocations. | Fiscal-year reassignment is guarded, but duplicate allocations can still be represented ambiguously by the grid. | Retain duplicate avoidance and document the actual fiscal-year guard in both Claims guides. | DOCUMENTED_VERIFIED |
| DOC-025 | Resolved: Monitor completion approval | Monitor completion delegates to the shared optional approval-submission transition and the core workflow section displays the runtime. | Approval configuration now participates in the completion path. | Both Monitor guides describe the current completion/workflow contract. | DOCUMENTED_VERIFIED |
| DOC-026 | Resolved: Obsolete Monitor edit-status model | Monitor writes preserve the selected Agency business status; workflow transitions apply configured IDs. | Edits do not imply a universal `draft` to `inprogress` progression. | Explain business status, completion evidence, and workflow state separately. | DOCUMENTED_VERIFIED |
| DOC-027 | Generated-document department context mismatch | `buildAgreementDocumentContext` in `server/utils/document-generation.ts` derives `agency.name` from the agreement but hard-codes `department.name`, `department.legalName`, and `department.address` to Health Canada values. | Templates using `department.*` can produce incorrect legal/identity text for an agreement owned by another configured agency. | Warn users and template authors that these tags are not agency-derived and require independent correction or an authorized replacement context before issuance outside Health Canada. | DOCUMENTED_VERIFIED |
| DOC-028 | Generated-document storage cleanup remains non-transactional | Generated-document deletion soft-deletes metadata before best-effort provider-object removal. The shared-attachment cleanup outbox is not a universal guarantee for generated/template files. | Provider or process failure can leave inaccessible bytes, and uncoordinated restores can create drift. | Document provider-aware paired backups, cleanup monitoring, and reconciliation without claiming filesystem-only storage or guaranteed outbox recovery. | DOCUMENTED_VERIFIED |
| DOC-029 | Document context-provider registration gap | `buildAgreementDocumentContext` consumes `event.context.documentGenerationContextProviders`, but a source-wide search finds no host module/plugin or installed-extension registrar, and the extension SDK declares no such capability. | Existing developer text could imply that extensions can currently register document context through a supported public contract. | Describe this as an event-local internal integration seam only and do not promise it as an extension SDK capability. | DOCUMENTED_VERIFIED |
| DOC-030 | Resolved in part: runtime resolver behavior | `extension-runtime-route.ts` suppresses an Agency/Proponent slot when its resolver returns false or null. Stream slots use persisted stream enablement/configuration without invoking that resolver. | Agency resolver suppression now works; it is not the stream-slot availability mechanism. | Describe both paths accurately in Concepts and SDK authoring guides. | DOCUMENTED_VERIFIED |
| DOC-035 | Resolved: Home dashboard placeholders | `app/pages/index.vue` now mounts `HomeWorkDashboard.vue`, which reads assigned and group work from `/api/assigned-work` and `/api/group-work`; the former static statistics and inert controls are gone. | Home totals and work lists are backed by the current queue responses, subject to their authorization and loading states. | Both Navigation guides describe the live dashboard, pagination, claiming, and recovery from failed loads. | DOCUMENTED_VERIFIED |
| DOC-036 | Explicit standard workflow selection | Standard workflow catalogues list eligible published setups and require an explicit selection. Other automatic workflow-purpose resolution remains a separate path. | The old blanket highest-ID selection description would hide the standard-workflow choice. | Both Workflow guides explain explicit standard selection, completion dispatch, and historical retry pins. | DOCUMENTED_VERIFIED |
| DOC-037 | Resolved: Amendment cancellation lock mismatch | The dedicated cancellation path permits cancellation of the active submission and closes the Amendment; without a run it resolves the applicable published cancellation status. | The old required two-step workflow-then-Amendment workaround is obsolete. | Both Amendment guides describe the supported Cancel action and its failure conditions. | DOCUMENTED_VERIFIED |
| DOC-038 | Closed Agreement guard contract | `agreement-write-transaction.ts` enforces the Closeout/terminal parent boundary before ordinary mutations; review/runtime writes resolve their business target through fresh-authorized domain helpers. | Current documentation must describe the enforced parent boundary rather than preserve an old issue number as proof of a live bypass. | Document the parent lock and current child retry/action prerequisites. No concurrency regression execution is claimed for this documentation run. | DOCUMENTED_VERIFIED |



These source-backed dispositions describe the pinned target `c02d847de7078a2110f54c298ed3ef7f3e1b620b`; unchanged findings retain source review from the prior baseline. `DOCUMENTED_VERIFIED` describes documentation coverage, not application regression execution. See [review scope](./sync-review.md) and [verification receipt](./source-baseline.md). No application code was patched in the reference clone.
