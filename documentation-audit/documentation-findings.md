# Documentation findings

| ID | Category | Evidence | Impact | Remediation | Status |
| --- | --- | --- | --- | --- | --- |
| DOC-001 | Missing audit controls | Documentation baseline has no `documentation-audit/` directory or coverage ledgers. | Completion cannot be mechanically demonstrated. | Add durable inventories, status, baseline, findings, and verification. | DOCUMENTED_VERIFIED |
| DOC-002 | Missing verification command | Baseline `package.json` exposes only VitePress dev/build/preview scripts. | Locale parity, links, anchors, redirects, source coverage, and terminal status are unchecked. | Implement `bun run docs:check`. | DOCUMENTED_VERIFIED |
| DOC-003 | Audience coverage gap | Existing navigation has user/admin/concept/developer material but no clearly identified operator section. | Deployment, backup, monitoring, security, and troubleshooting guidance is incomplete. | Add bilingual operator documentation and navigation. | DOCUMENTED_VERIFIED |
| DOC-004 | API reference gap | Existing developer route page groups only selected routes while the source registers 428 handlers. | Individual route contracts lack explicit documentation disposition. | Add grouped bilingual API references and reconcile every handler ledger row. | DOCUMENTED_VERIFIED |
| DOC-005 | Data reference gap | Existing site has no exhaustive migration/entity/integrity reference. | Constraints, triggers, precision, polymorphism, concurrency, and PostgreSQL/PGlite differences are incomplete. | Add bilingual data-model references and material mechanism inventory rows. | DOCUMENTED_VERIFIED |
| DOC-006 | Agreement similarity UI limitation | `server/api/agreements/index.post.ts` and `server/utils/agreement-profile-routes.ts` require confirmation fingerprints for near Funding History matches, while `app/pages/agreements/new.vue`, `app/pages/agreements/[id].vue`, and the agreement profile components neither render the similarity dialog nor submit confirmations. | A near external-history match can block Agreement create/update with no supported confirmation action in the core form. | Document the current failure and safe recovery path in both Agreement overview pages; do not imply that users can override it through the UI. | DOCUMENTED_VERIFIED |
| DOC-007 | Agreement budget fiscal-year update limitation | `server/api/agreements/[id]/budget-fiscal-years/[childId].patch.ts` delegates to `patchAgreementBudgetFiscalYear` in `server/utils/agreement-budget.ts`; unlike creation and the lookup, that helper does not call the agreement-duration overlap guard, block downstream financial use, or revalidate existing line capacity after changing the underlying fiscal year. The earlier application audit route ledger overstates these protections. | A direct PATCH can move the stable fiscal-year row outside the agreement duration or change the fiscal-year meaning beneath populated/downstream records; the normal picker prevents the first case but is not a server guarantee. | Document the current API/UI distinction, prohibit operational changes after downstream use, and provide a safe stop-and-reconcile recovery path in both Agreement Budget guides. | DOCUMENTED_VERIFIED |
| DOC-008 | Agreement amendment approval UI gap | `app/pages/agreements/[id]/amendments/[amendmentId].vue` renders the Recommendation tab as headings and explanatory text only; it does not mount the shared recommendation or approval controls. `server/utils/agreement-amendment-approval-runtime.ts` supports materialization and decisions through generalized APIs. | Core UI users can prepare/cancel a draft but cannot submit it for approval or view/act on amendment approval steps from the amendment page. | Document the placeholder boundary and do not instruct users to treat it as an approval workflow; identify authorized supported integration as the only current route to the server runtime. | DOCUMENTED_VERIFIED |
| DOC-009 | Amendment budget capacity gap | Amendment budget-line create/patch handlers validate the draft/type/version, cost-line ownership, and shared money schema but do not call `assertAgreementBudgetProgramFundingCapacity`; `promoteApprovedAgreementAmendment` also lacks the cross-agreement stream-capacity check used by current-budget writes. | A draft snapshot and its promotion can exceed the stream budget plus overcommit threshold even though equivalent edits in the ordinary Budget tab are rejected. | Warn both audiences that successful amendment snapshot persistence is not capacity evidence and require manual capacity reconciliation before approval. | DOCUMENTED_VERIFIED |
| DOC-010 | Agreement revision runtime gap | Migration `0009_funding_case_agreement.ts` defines `Funding_Case_Agreement_Revision` and its checkpoint constraints, but no `server/api/**` or `server/utils/**` path inserts it; `promoteApprovedAgreementAmendment` only promotes dates/version copies and marks the amendment approved. | Documentation could falsely imply that final amendment approval persists the database revision checkpoint described by the schema. | Distinguish actual amendment/version lineage from the unwritten revision table in both Amendment guides. | DOCUMENTED_VERIFIED |
| DOC-011 | Commitment completion/approval disconnect | `executeAgreementCommitmentCompletion` in `server/utils/agreement-commitment-completion.ts` always sets the commitment to `complete` and active and never resolves or materializes an approval template. The generic approval runtime exists in `server/utils/agreement-commitment-approval-runtime.ts`, but `app/pages/agreements/[id]/commitments/[commitmentId].vue` mounts completion and workflow sections only. | The previous guide falsely promised that a configured stream approval template would make completion create a routing slip and show approval controls. | Document direct completion as the core UI behaviour and identify commitment approval as an API/integration capability that requires an explicit caller. | DOCUMENTED_VERIFIED |
| DOC-012 | Commitment amount sign validation gap | `FundingCaseAgreementCommitmentLineBaseSchema` uses `RequiredMoney()`, not `RequiredPositiveMoney()`, and the `numeric(19,2)` column has no non-negative check constraint. The UI amount control also has no minimum. | Zero or negative commitment lines can be saved even though they ordinarily represent a positive financial commitment, and can distort displayed totals and capacity checks. | State the exact accepted numeric contract and warn operators to apply agency review controls rather than claiming the screen enforces positive amounts. | DOCUMENTED_VERIFIED |
| DOC-013 | Forecast completion/approval disconnect | `executeAgreementForecastCompletion` always sets the forecast header to `complete` and does not resolve a template, create a routing slip, or activate the forecast. Generic approval support exists in `agreement-forecast-approval-runtime.ts`, while the detail page mounts no approval component. | The previous guide falsely claimed completion conditionally moved forecast lines to approval and that line statuses drove the lifecycle; forecast lines have no status column. | Document header-level direct completion and inactive result, and identify approval as a separately invoked API/integration capability. | DOCUMENTED_VERIFIED |
| DOC-014 | Forecast header/version uniqueness gap | The migration has only a partial unique index for an *active* agreement/fiscal-year header and no unique constraint on forecast-line `(forecast, budget line, month, version)`. The UI's grouped actions select the first header and its detail cell map overwrites duplicates. | Multiple inactive headers or duplicate monthly coordinates can be created through the API and then be represented ambiguously or incompletely by the core UI. | Document that versions are line labels rather than entities and require one header and one line per logical coordinate as an operational invariant. | DOCUMENTED_VERIFIED |
| DOC-015 | Forecast fiscal-year reassignment gap | The forecast-header PATCH validates the destination current fiscal year but does not revalidate or migrate existing lines. The detail page filters budget rows to the new year while completion merely checks for any active line. | Old-year lines can become invisible in the new grid yet continue to satisfy the completion line requirement and remain attached to the header. | Warn against fiscal-year changes after data entry and require authorized reconciliation before completion if reassignment occurred. | DOCUMENTED_VERIFIED |
| DOC-016 | Forecast grid non-atomicity and amount-sign gap | The page saves up to 12 cells per current budget line as sequential individual POST/PATCH requests. The UI input has `min=0`, but the route schema uses `RequiredMoney()` and the database has no non-negative check. | A late request failure can leave earlier cells saved, and a direct API client can persist negative forecast amounts despite the UI affordance. | Document partial-save recovery and distinguish the UI minimum from the server/database contract. | DOCUMENTED_VERIFIED |
| DOC-017 | Payment completion/approval disconnect | `executeAgreementPaymentCompletion` in `server/utils/agreement-payment-completion.ts` always writes `complete` and starts the completion workflow without resolving a payment approval template or materializing a routing slip. `server/utils/agreement-payment-approval-runtime.ts` provides a separately callable runtime, but `app/pages/agreements/[id]/payments/[paymentId].vue` mounts only completion and workflow sections. | The previous guide falsely claimed that completion conditionally entered `pendingapproval` and that approval controls appeared on the payment page. | Document direct completion as the core UI path and identify payment approval as an API/integration capability requiring an explicit caller. | DOCUMENTED_VERIFIED |
| DOC-018 | Payment commitment lookup eligibility mismatch | `payments/lookups/commitments.get.ts` includes every `complete` commitment regardless of `egcs_fc_active`, while `resolveActiveAgreementPaymentCommitmentByType` requires both `complete`/`approved` status and active true and resolves by commitment type rather than the selected record ID. | The picker can display an inactive completed commitment that fails on save or causes another active commitment of the same type to be selected. | Warn users that picker presence is not eligibility proof and require verification of the active commitment before payment creation. | DOCUMENTED_VERIFIED |
| DOC-019 | Payment operational-state implementation gap | Payment enums and lock sets include `pay`, `wait`, `processed`, and `paid`, but a source-wide executable-path search found no current core route or installed-extension writer for those states. Automated Payments calculates and enforces a create-time ceiling only. | Documentation could imply an automated disbursement-processing lifecycle that the current application does not implement. | Describe these values as recognized locked states only and do not promise automatic progression. | DOCUMENTED_VERIFIED |
| DOC-020 | Claim-reconciliation completion/approval disconnect | `executeAgreementClaimReconcileCompletion` always writes `complete` and starts a workflow without resolving or materializing an approval template. A separate generic runtime exists in `agreement-claim-reconcile-approval-runtime.ts`, but the claim detail page mounts completion and workflow controls only. | Configuring a `fundingclaimreconcile` template does not cause core-UI completion to enter `pendingapproval`, and users have no mounted approval controls. | Document direct completion and the explicit API/integration approval boundary in both Claims guides. | DOCUMENTED_VERIFIED |
| DOC-021 | Claim grid atomicity and financial-validation gap | Submission and reconciliation saves issue sequential per-line POST/PATCH requests. The UI uses non-negative controls, while `RequiredMoney`/`OptionalMoney` and the database accept signed submitted, reconciled, and sampled values; completion checks only that a reconciliation line exists. | A late request failure leaves a partial grid, and direct clients can save negative or internally inconsistent amounts without blocking completion. | Document refresh-and-compare recovery and distinguish displayed totals/minima from enforced server rules. | DOCUMENTED_VERIFIED |
| DOC-022 | Claim fiscal-year grouping action ambiguity | `AgreementClaimsTab.vue` groups by fiscal-year display and `getGroupedClaimId` returns the first leaf for group-level edit/delete. Individual rows expose only open navigation. | With multiple claims in one fiscal year, edit/delete can target a different claim than the user expects. | Instruct users to open the individual claim for unambiguous work and warn against relying on group actions. | DOCUMENTED_VERIFIED |
| DOC-023 | Claim fiscal-year reassignment and line uniqueness gaps | Claim PATCH validates a new current fiscal year but does not migrate or revalidate existing allocated lines. The migration has no active `(claim, budget line)` uniqueness constraint, and the editable grid selects the first matching line. | Old-year lines can disappear from the Submission grid while still participating in reconciliation, and duplicate logical lines can be represented incompletely while editable. | Prohibit operational fiscal-year changes after line entry, require reconciliation if one occurred, and document duplicate-line avoidance. | DOCUMENTED_VERIFIED |
| DOC-024 | GC Forms claim status documentation drift | `materializeGcFormsClaimSubmission` inserts the claim with status `submitted`, while the installed extension README still says it creates a draft claim. The paired site extension guides already describe a submitted claim. | Copying the README claim would teach an incorrect post-import lifecycle and obscure the one-time submitted-line allocation path. | Use executable materialization source as authority and retain the correct submitted-state guidance in both the Claims and GC Forms pages. | DOCUMENTED_VERIFIED |
| DOC-025 | Monitor completion/approval disconnect | `executeAgreementMonitorCompletion` always writes `complete`, creates the common completion, and starts the completion workflow without resolving or materializing the configured monitor approval template. The generic runtime in `agreement-monitor-approval-runtime.ts` is separate, and the monitor page's workflow component cannot create an initial standalone routing slip. | The previous guide falsely claimed that completion conditionally entered `pendingapproval` and exposed monitor approval controls. | Document direct completion, the separate explicit API/workflow approval boundary, and the absence of an initial standalone approval action in the core monitor page. | DOCUMENTED_VERIFIED |
| DOC-026 | Monitor edit-status implementation gap | `syncAgreementMonitorEditingStatus` would move an editable monitor to `inprogress`, but a source-wide call search finds only its declaration; monitor header and child-resource routes never invoke it. | The previous guide falsely claimed that sub-record edits advance the status, so users may interpret a content-rich `draft` monitor as untouched or wait for an automatic transition that never occurs. | State that `draft` persists through edits unless another explicit lifecycle path changes it, and warn users to treat status as the stored value rather than an activity indicator. | DOCUMENTED_VERIFIED |
| DOC-027 | Generated-document department context mismatch | `buildAgreementDocumentContext` in `server/utils/document-generation.ts` derives `agency.name` from the agreement but hard-codes `department.name`, `department.legalName`, and `department.address` to Health Canada values. | Templates using `department.*` can produce incorrect legal/identity text for an agreement owned by another configured agency. | Warn users and template authors that these tags are not agency-derived and require independent correction or an authorized replacement context before issuance outside Health Canada. | DOCUMENTED_VERIFIED |
| DOC-028 | Generated-document storage cleanup is non-transactional | Generated-document metadata and filesystem bytes cannot share a transaction. Delete commits `_deleted` for the generated row and attachment before best-effort object removal; cleanup failure is logged and success is still returned. Generation rollback covers insert failures but cannot guarantee cleanup after a process/commit boundary failure. | Orphaned, inaccessible bytes can remain in private storage and database-only or storage-only backup/restore can create drift. | Document paired backups, cleanup-error monitoring, storage reconciliation, and the prohibition on manually restoring rows by clearing soft-delete flags. | DOCUMENTED_VERIFIED |
| DOC-029 | Document context-provider registration gap | `buildAgreementDocumentContext` consumes `event.context.documentGenerationContextProviders`, but a source-wide search finds no host module/plugin or installed-extension registrar, and the extension SDK declares no such capability. | Existing developer text could imply that extensions can currently register document context through a supported public contract. | Describe this as an event-local internal integration seam only and do not promise it as an extension SDK capability. | DOCUMENTED_VERIFIED |
| DOC-030 | Extension runtime-resolver enforcement gap | `resolveAgencyRuntimeResponse` calls `resolveExtensionRuntimeSlot`, but still emits the registered slot with `{}` when that helper returns `null`; `resolveStreamRuntimeResponse` never invokes the resolver and uses persisted stream rows directly. | The manifest and prior guidance imply that a runtime resolver can dynamically suppress slot availability, while the current host cannot suppress an agency/Proponent slot that way and does not consult it for stream slots. | Document the exact executable limitation and require agency/stream enablement plus host RBAC as the current suppression controls; do not promise resolver-only hiding. | DOCUMENTED_VERIFIED |
| DOC-031 | Automated Payments disabled-type UI mismatch | `calculateAutomatedPaymentFromDb` returns `enabled: false` with zero amounts for a payment type excluded by stream config. `AutomatedPaymentAmountCalculator.vue` ignores the flag and publishes the zero suggestion, and `AgreementPaymentsTab.vue` copies every finite suggestion into Amount; the before-create hook correctly skips ceiling enforcement when disabled. | Selecting a configured-off type can unexpectedly replace a user's draft amount with zero and present a zero ceiling even though the extension does not govern that type. | Document the current behaviour and recovery, and distinguish it from server enforcement; do not claim the calculator disappears per selected type. | DOCUMENTED_VERIFIED |
| DOC-032 | GC Forms preview RBAC declaration mismatch | The manifest declares `/streams/[streamId]/preview` with `transfer_payment:read`, but `preview.post.ts` calls `authorizeGcFormsStream(context, streamId, 'update')` before computing the non-persistent preview. | A reader who satisfies dispatcher RBAC still receives a forbidden response from the extension-level check, contrary to the manifest-level contract. | Document effective update access for preview and do not advertise it as a read-only-permission operation, while preserving that it performs no persistence. | DOCUMENTED_VERIFIED |
| DOC-033 | Narrative Quality assessment-target enablement documentation drift | The extension README and direct-handler unit case say assessment-target discovery does not require enablement rows, but the externally reachable catch-all dispatcher enforces agency and stream enablement for this stream-RBAC route before invoking the handler. | An administrator following the package README can expect assessment targets before stream enablement and instead receive `EXTENSION_STREAM_DISABLED`. | Document the externally reachable two-switch requirement and distinguish direct handler behaviour from dispatch; do not copy the README claim into the site guide. | DOCUMENTED_VERIFIED |
| DOC-034 | Narrative Tags Proponent resolver enforcement gap | The resolver computes lead-agency and readable linked-stream sources, but the host agency runtime path skips all resolvers unless the lead agency is enabled and emits a registered slot even when the resolver returns null. | The package README and earlier guide promise lead-agency **or** linked-agreement availability, which the mounted host cannot enforce; users can miss a valid linked source or see an empty slot. | Document actual slot visibility separately from the source-filtered Proponent GET contract and do not promise linked-stream-only activation. | DOCUMENTED_VERIFIED |

All findings have a source-backed disposition and verified bilingual destination coverage. The application discrepancies recorded here remain application behaviour; they were documented rather than patched.
